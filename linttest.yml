---
- hosts: localhost
  tasks:
    - name: test lint
      shell: ansible-lint /tests/test.yml
      ignore_errors: true
      register: out
    
    - name: sleep
      shell: sleep 99999999

#    - debug:
#        var: out
