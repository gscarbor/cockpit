---
- hosts: localhost
  tasks:
    - name: test lint
      shell: ansible-lint /tests/test.yml
      ignore_errors: true
      register: out
      
    - name: uptime
      shell: ls . jjjj

#    - debug:
#        var: out
